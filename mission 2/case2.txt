input integer a


if a=1 or 0 or -1
	print "a is not prime"
else
	for 1<=i<=a/2 //어떤 수의 절반까지의 약수가 1밖에 없을 경우 그 수는 소수이다. 
		count=count number of divisor of a
	if count=1
		print "a is prime"
	else
		print "a is not prime"



